

services:
  restweb:
    build:
      context: .  # The context is the root of the project
      dockerfile: ./web/Dockerfile  # Correct path to the Dockerfile
    ports:
      - "8080:80"  # Map port 8080 on host to port 80 in container
#    networks:
#      - front-tier
#      - back-tier
#    depends_on:
#      - backend

#  backend:
#    image: backend-restaurant-app  # This is your backend image name
#    ports:
#      - "8082:8080"  # Ensure this matches your backend setup
#    networks:
#      - back-tier
#
#networks:
#  front-tier: {}
#  back-tier:
#    external: true  # Ensure the backend network exists
